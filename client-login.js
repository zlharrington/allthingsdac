(()=>{const form=document.getElementById('loginForm');const statusEl=document.getElementById('loginStatus');if(!form||!statusEl)return;async function tryLogin(url,payload){const response=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload),credentials:'same-origin'});const data=await response.json().catch(()=>({}));return{response,data};}form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button[type="submit"]');button.disabled=true;statusEl.textContent='Signing in…';statusEl.classList.remove('error');const payload={username:document.getElementById('username').value,password:document.getElementById('password').value};try{let result=await tryLogin('/api/admin/owner-login',payload);if(!result.response.ok&&(result.response.status===401||result.response.status===503))result=await tryLogin('/api/admin/login',payload);if(!result.response.ok)throw new Error(result.data.error||`Login failed (${result.response.status}).`);window.location.replace('/admin/');}catch(error){statusEl.textContent=error.message;statusEl.classList.add('error');}finally{button.disabled=false;}});})();