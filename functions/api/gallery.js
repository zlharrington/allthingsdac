function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'public, max-age=60'}});}
export async function onRequestGet(context){let cursor;const photos=[];do{const page=await context.env.GALLERY_BUCKET.list({prefix:'gallery/',limit:1000,cursor,include:['customMetadata','httpMetadata']});for(const object of page.objects){const meta=object.customMetadata||{};if(meta.published!=='true')continue;const file=object.key.slice('gallery/'.length);photos.push({file,url:`/media/${encodeURIComponent(file)}`,title:meta.title||'',alt:meta.alt||'All Things Drywall & Construction project photo',category:meta.category||'Project',createdAt:meta.createdAt||''});}cursor=page.truncated?page.cursor:undefined;}while(cursor);photos.sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt)));return json({photos});}
